# Python backend URL (for Next.js rewrites in dev)
BACKEND_URL=http://localhost:8000

# Public backend URL (used by browser for SSE streaming, bypasses Next.js proxy)
NEXT_PUBLIC_BACKEND_URL=http://localhost:8000
